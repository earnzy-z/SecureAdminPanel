<?xml version="1.0" encoding="utf-8"?>
<!-- Premium Glass Morphism Card with 3D Effect -->
<com.google.android.material.card.MaterialCardView
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/feature_card"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_margin="8dp"
    app:cardElevation="0dp"
    app:cardCornerRadius="24dp"
    app:cardBackgroundColor="@android:color/transparent"
    android:clickable="true"
    android:focusable="true"
    android:stateListAnimator="@animator/card_3d_elevation">

    <FrameLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content">

        <!-- Multi-layer shadow effect -->
        <View
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:layout_marginTop="4dp"
            android:layout_marginStart="2dp"
            android:layout_marginEnd="2dp"
            android:background="@drawable/card_shadow_multi_layer"
            android:alpha="0.3" />

        <!-- Glass morphism background -->
        <androidx.constraintlayout.widget.ConstraintLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:background="@drawable/glass_morphism_card_bg"
            android:padding="20dp"
            android:clipToOutline="true">

            <!-- Gradient accent bar -->
            <View
                android:id="@+id/accent_bar"
                android:layout_width="4dp"
                android:layout_height="0dp"
                android:background="@drawable/gradient_accent_vertical"
                app:layout_constraintTop_toTopOf="parent"
                app:layout_constraintBottom_toBottomOf="parent"
                app:layout_constraintStart_toStartOf="parent" />

            <!-- Icon container with gradient background -->
            <FrameLayout
                android:id="@+id/icon_container"
                android:layout_width="64dp"
                android:layout_height="64dp"
                android:layout_marginStart="12dp"
                app:layout_constraintTop_toTopOf="parent"
                app:layout_constraintStart_toEndOf="@id/accent_bar"
                app:layout_constraintEnd_toEndOf="parent">

                <!-- Gradient circle background -->
                <View
                    android:layout_width="match_parent"
                    android:layout_height="match_parent"
                    android:background="@drawable/circle_gradient_premium" />

                <!-- Icon with shadow -->
                <ImageView
                    android:id="@+id/feature_icon"
                    android:layout_width="36dp"
                    android:layout_height="36dp"
                    android:layout_gravity="center"
                    android:scaleType="fitCenter"
                    android:src="@drawable/ic_daily_bonus"
                    android:elevation="2dp" />

                <!-- Shine effect -->
                <View
                    android:layout_width="match_parent"
                    android:layout_height="match_parent"
                    android:background="@drawable/shine_effect_oval"
                    android:alpha="0.3" />

            </FrameLayout>

            <!-- Content section -->
            <LinearLayout
                android:id="@+id/content_section"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:orientation="vertical"
                android:layout_marginTop="16dp"
                app:layout_constraintTop_toBottomOf="@id/icon_container"
                app:layout_constraintStart_toStartOf="@id/icon_container"
                app:layout_constraintEnd_toEndOf="parent">

                <!-- Title with premium typography -->
                <com.google.android.material.textview.MaterialTextView
                    android:id="@+id/feature_title"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:text="Feature"
                    android:textSize="16sp"
                    android:textStyle="bold"
                    android:textColor="@color/text_primary"
                    android:letterSpacing="0.02"
                    android:gravity="center"
                    android:maxLines="2"
                    android:ellipsize="end"
                    android:fontFamily="sans-serif-medium" />

                <!-- Subtitle with gradient text effect -->
                <com.google.android.material.textview.MaterialTextView
                    android:id="@+id/feature_subtitle"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:text="Action"
                    android:textSize="12sp"
                    android:textColor="@color/primary"
                    android:gravity="center"
                    android:layout_marginTop="6dp"
                    android:alpha="0.9"
                    android:fontFamily="sans-serif" />

            </LinearLayout>

            <!-- Premium badge (optional) -->
            <View
                android:id="@+id/premium_badge"
                android:layout_width="8dp"
                android:layout_height="8dp"
                android:background="@drawable/circle_premium_badge"
                android:visibility="gone"
                app:layout_constraintTop_toTopOf="parent"
                app:layout_constraintEnd_toEndOf="parent" />

            <!-- Shimmer overlay for loading state -->
            <com.facebook.shimmer.ShimmerFrameLayout
                android:id="@+id/shimmer_overlay"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:visibility="gone"
                app:shimmer_auto_start="false"
                app:shimmer_base_alpha="0.3"
                app:shimmer_highlight_alpha="0.6"
                app:shimmer_duration="1200"
                app:shimmer_direction="left_to_right">

                <View
                    android:layout_width="match_parent"
                    android:layout_height="match_parent"
                    android:background="@android:color/white" />

            </com.facebook.shimmer.ShimmerFrameLayout>

        </androidx.constraintlayout.widget.ConstraintLayout>

    </FrameLayout>

</com.google.android.material.card.MaterialCardView>

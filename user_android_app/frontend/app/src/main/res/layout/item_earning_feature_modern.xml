<?xml version="1.0" encoding="utf-8"?>
<!-- Modern Neumorphism Style Card with Floating Effect -->
<com.google.android.material.card.MaterialCardView
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/feature_card"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_margin="10dp"
    app:cardElevation="8dp"
    app:cardCornerRadius="28dp"
    app:cardBackgroundColor="@color/white"
    android:clickable="true"
    android:focusable="true"
    android:foreground="?attr/selectableItemBackground"
    android:stateListAnimator="@animator/card_lift_animator">

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:padding="24dp">

        <!-- Colored accent strip on top -->
        <View
            android:id="@+id/color_accent"
            android:layout_width="60dp"
            android:layout_height="4dp"
            android:background="@drawable/gradient_primary_horizontal"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintStart_toStartOf="parent" />

        <!-- Icon with modern circular background -->
        <FrameLayout
            android:id="@+id/icon_frame"
            android:layout_width="72dp"
            android:layout_height="72dp"
            android:layout_marginTop="16dp"
            app:layout_constraintTop_toBottomOf="@id/color_accent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent">

            <!-- Outer glow ring -->
            <View
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:background="@drawable/circle_glow_ring"
                android:alpha="0.2" />

            <!-- Inner gradient circle -->
            <View
                android:layout_width="64dp"
                android:layout_height="64dp"
                android:layout_gravity="center"
                android:background="@drawable/circle_gradient_soft" />

            <!-- Icon -->
            <ImageView
                android:id="@+id/feature_icon"
                android:layout_width="40dp"
                android:layout_height="40dp"
                android:layout_gravity="center"
                android:scaleType="fitCenter"
                android:src="@drawable/ic_daily_bonus"
                android:tint="@color/white" />

        </FrameLayout>

        <!-- Title -->
        <com.google.android.material.textview.MaterialTextView
            android:id="@+id/feature_title"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:text="Feature Title"
            android:textSize="18sp"
            android:textStyle="bold"
            android:textColor="@color/text_primary"
            android:gravity="center"
            android:maxLines="2"
            android:ellipsize="end"
            android:layout_marginTop="20dp"
            android:letterSpacing="0.01"
            android:fontFamily="sans-serif-medium"
            app:layout_constraintTop_toBottomOf="@id/icon_frame"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent" />

        <!-- Subtitle with icon -->
        <LinearLayout
            android:id="@+id/subtitle_container"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:gravity="center"
            android:layout_marginTop="8dp"
            app:layout_constraintTop_toBottomOf="@id/feature_title"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent">

            <ImageView
                android:layout_width="16dp"
                android:layout_height="16dp"
                android:src="@drawable/ic_star"
                android:tint="@color/primary"
                android:alpha="0.8" />

            <com.google.android.material.textview.MaterialTextView
                android:id="@+id/feature_subtitle"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Tap to Start"
                android:textSize="13sp"
                android:textColor="@color/primary"
                android:layout_marginStart="6dp"
                android:alpha="0.9"
                android:fontFamily="sans-serif" />

        </LinearLayout>

        <!-- Decorative corner element -->
        <View
            android:layout_width="40dp"
            android:layout_height="40dp"
            android:background="@drawable/corner_decoration"
            android:alpha="0.1"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintEnd_toEndOf="parent" />

    </androidx.constraintlayout.widget.ConstraintLayout>

</com.google.android.material.card.MaterialCardView>
